@model IEnumerable<Domain.Link>;

<table id="onlySitemapUrlsTable" class="table table-striped table-bordered">
    <thead class="thead-dark">
        <tr>
            <th scope="col">Url</th>
        </tr>
    </thead>
        <tbody>
            @foreach (var result in Model)
            {
                <tr>
                    <td>@result.Url</td>
                </tr>    
            }
        </tbody>
</table>